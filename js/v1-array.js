// To-Do List - Array Version 1.0

// Application should have a place to store todos.
var todos = ['item 1', 'item 2', 'item 3'] // Using variables to store a reference to our array.

// Application should have a way to display todos.
console.log('My todos:', todos) // console.log takes a number of arguments.

// Application should have a way to add new todos.
todos.push('new todo')

// Application should have a way to change a todo.
todos[0] = 'Changed!' // Changing the first item:

// Application should have a way to delete a todo.
todos.splice(0, 1) // Deleting the first item.
